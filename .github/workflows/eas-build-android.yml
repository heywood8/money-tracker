name: EAS Build Android

on:
  workflow_dispatch:
    inputs:
      profile:
        description: 'Build profile'
        required: true
        default: 'preview'
        type: choice
        options:
          - preview
          - production
  push:
    tags:
      - '*'
    branches:
      - main

jobs:
  build:
    name: EAS Build Android
    runs-on: ubuntu-latest

    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v6

      - name: 🏗 Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🚀 Build tag
        if: github.ref_type == 'tag'
        run: |
          PROFILE="${{ github.event.inputs.profile || 'preview' }}"
          echo "Building with profile: $PROFILE"
          eas build --platform android --profile $PROFILE --non-interactive --no-wait 2>&1 | tee eas-build-output.txt
          
          # Extract the build URL from EAS output
          BUILD_URL=$(grep -oE 'https://expo\.dev/accounts/[^[:space:]]+/builds/[a-f0-9-]+' eas-build-output.txt | head -1)
          echo "BUILD_URL=$BUILD_URL" >> $GITHUB_ENV

      - name: 🚀 Build main
        if: github.ref_type == 'tag'
        run: |
          PROFILE="${{ github.event.inputs.profile || 'preview' }}"
          echo "Building with profile: $PROFILE"
          eas update --platform android --profile $PROFILE --non-interactive --no-wait --auto 2>&1 | tee eas-build-output.txt
          
          # Extract the build URL from EAS output
          BUILD_URL=$(grep -oE 'https://expo\.dev/accounts/[^[:space:]]+/builds/[a-f0-9-]+' eas-build-output.txt | head -1)
          echo "BUILD_URL=$BUILD_URL" >> $GITHUB_ENV

      - name: Post build URL to summary
        run: |
          echo "## 🚀 EAS Build Started" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Build Profile:** ${{ github.event.inputs.profile || 'preview' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Build URL:** [$BUILD_URL]($BUILD_URL)" >> $GITHUB_STEP_SUMMARY
